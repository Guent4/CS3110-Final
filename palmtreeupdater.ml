open Coconuts
open Hashtbl

(* let gen_hash () = string_of_int (hash (Unix.gettimeofday()))

let update_tree (cmd:cmd_expr) (pt:tree_node) : tree_node =
  match cmd with
  | (_,INIT,_,_) -> Commit (gen_hash (), "initial commit")
  | _ -> failwith "not implemented" *)